# Change Log

All source edits for this reproduction task are confined to
`/scratch/08384/tg876840/tmp/b-1D_PB/reproduce3d`.

## 2026-06-29

- Added `tests/input_audit.py` and `src/vasp_inputs.py` to audit allowed VASP inputs.
- Added `tests/compare_volumetric_outputs.py` to compare regenerated volumetric fields with reference outputs.
- Copied CEP-DIP source into `code/CEP-DIP` for local-only diagnostics and future fixed-density reproduction work.
- Edited `code/CEP-DIP/src/solvation.F` inside `SOL_VCORRECTION`: when solvent field writing is enabled, write the PB inputs `DBG_NVAL`, `DBG_DENCOR`, `DBG_NE`, and `DBG_PHISOL_IN` immediately before the solver call. These files are diagnostic outputs only and are not allowed as inputs to the final reproduction workflow.
- Built the local patched CEP-DIP copy. `make std` linked `build/std/vasp`; the final copy step failed only because `bin/` did not exist, so `bin/` was created and `build/std/vasp` was copied to `bin/vasp_std`.
- Submitted fixed-density diagnostic runs `work/cal18_dbgchg_nsw10_dbg` and `work/cal18_dbgchg_nsw5_dbg` using the patched local binary. These runs read a converged CHGCAR and write diagnostic PB inputs for comparison with the full-SCF diagnostic run.
- Added `tests/compare_debug_fields.py`, a read-only comparison helper for diagnostic volumetric fields such as `DBG_NVAL`, `DBG_DENCOR`, `DBG_NE`, and `DBG_PHISOL_IN`.
- Temporarily changed `code/CEP-DIP/src/solvation.F` local diagnostic copy: `tol_init` in `SOL_VCORRECTION` from `1.e-3_q` to `1.e-8_q`. This is a test of whether the fixed-density postprocessing mismatch is caused by the initial PB solve stopping too early; it must be reverted if the test is not useful.
- Submitted `work/cal18_fixed_tol1e8_nsw0`, a one-shot fixed-density diagnostic run with `NELM=0`, `NSW=0`, and the stricter local PB `tol_init`.
- Stopped `work/cal18_fixed_tol1e8_nsw0` after it produced an invalid cavity/input state (`q_sol` about 0.5 and essentially zero `qion_max`). Reverted `tol_init` from `1.e-8_q` back to `1.e-3_q`; the stricter tolerance is not the fix.
- Found that `work/cal18_fixed_tol1e8_nsw0` was also generated without an explicit `ICHARG=11`, so it followed the wrong charge-density initialization path. The run is invalid and should not be used as evidence about PB convergence.
- Rebuilt the local diagnostic binary after restoring `tol_init=1.e-3_q`.
- Submitted `work/cal18_fixed_ichg11_nsw1_dbg`, a corrected one-step fixed-density diagnostic with explicit `ICHARG=11`, `NELM=0`, `NSW=1`, and diagnostic PB input writing.
- Edited `code/CEP-DIP/src/main.F` to preserve the density read from `CHGCAR` for fixed-density calculations: added `CHTOT_READ` and `LHAVE_CHTOT_READ`, saved `CHTOT` immediately after successful `READCH` with `ICHARG>10`, and restored `CHTOT_READ` before the final `LVHAR/LVTOT` `POTLOK` call and before `CDIPOL_CHTOT_REC`. This is intended to prevent the fixed-density postprocessing path from using a charge density regenerated by the electronic step instead of the input `CHGCAR`.
- Rebuilt the local diagnostic binary after the fixed-density `CHTOT_READ` patch.
- Submitted `work/cal18_fixed_restore_nsw1_dbg`, the same one-step fixed-density diagnostic as `work/cal18_fixed_ichg11_nsw1_dbg` but using the patched binary that restores `CHTOT_READ` before final potential/solvent output.
- Inspection of `READCH` showed that it reads the old atom positions from the CHGCAR header, subtracts overlapping atomic charge density at those old positions, and adds overlapping atomic charge density at the current POSCAR positions. Therefore a POSCAR/CHGCAR coordinate mismatch changes the internal `CHTOT` even when the CHGCAR grid itself is unchanged.
- Submitted `work/cal18_fixed_contcar_nsw1_dbg`, identical to the fixed-density diagnostic but using `CONTCAR` from `work/cal18_full_scf_dbg` as `POSCAR` so that the current positions match the CHGCAR header.
- Added temporary diagnostics in the local code: `DBG_READCH_CHTOT` in `main.F` after successful fixed-density `READCH`, and `DBG_INCHG_CHTOT` in `fileio.F` immediately after `INCHG` and before the `READCH` old-position/new-position atomic-density correction.
- The diagnostics showed `INCHG` reproduces the CHGCAR first grid to about `1e-12`, while `READCH` differed by about `0.0588` raw RMSE when POSCAR coordinates did not exactly match the CHGCAR header.
- Generated `work/cal18_headerpos_dump/POSCAR` directly from the CHGCAR header. With these exact header coordinates, `DBG_READCH_CHTOT` matches the CHGCAR first grid with raw RMSE about `6.5e-11`; this identifies the fixed-density density mismatch as the `READCH` old-position/new-position correction applied to slightly different coordinates.
- Submitted `work/cal18_headerpos_full_nsw1`, a full one-step fixed-density diagnostic using the CHGCAR-header POSCAR, to test whether the solvent outputs now reproduce the full-SCF reference.
- Stopped `work/cal18_headerpos_full_nsw1` because the temporary early `OUTCHG` diagnostics created `fort.97` under MPI and were no longer needed.
- Removed the temporary `DBG_READCH_CHTOT` and `DBG_INCHG_CHTOT` source edits, leaving only the fixed-density `CHTOT_READ` preservation/restoration patch and the `SOL_VCORRECTION` final PB input diagnostics.
- Rebuilt the local diagnostic binary after removing the temporary early read diagnostics.
- Submitted `work/cal18_headerpos_clean_nsw1`, a clean full one-step fixed-density diagnostic using the CHGCAR-header POSCAR and no early read-dump code.
- `work/cal18_headerpos_clean_nsw1` showed that exact CHGCAR-header coordinates fix the input density and cavity (`DBG_NVAL` raw RMSE about `1e-10`, cavity fields about `1e-13`), but `DBG_PHISOL_IN` remains wrong by about `0.164 eV`. The remaining mismatch is therefore in the solute potential/dipole state entering the PB solver, not in the density or cavity.
- Submitted `work/cal18_headerpos_clean_nsw10`, using the CHGCAR-header POSCAR and `NSW=10`, to test whether repeated fixed-density outer steps can converge the solvent/dipole state while the `CHTOT_READ` patch preserves the exact input density.
- `work/cal18_headerpos_clean_nsw10` showed that repeated outer steps converge the dipole/solute-potential input substantially (`DBG_PHISOL_IN` raw RMSE about `3.7e-4 eV`), but `DBG_NE` still differs because using CHGCAR-header coordinates also truncates the position-dependent `DENCOR` and local ionic-potential ingredients.
- Edited `code/CEP-DIP/src/fileio.F`: added optional `LSKIP_POSCORR` to `READCH`; when true, `READCH` skips the old-position/new-position overlapping atomic-density correction after `INCHG`.
- Edited `code/CEP-DIP/src/main.F`: the `READCH` call now passes `INFO%ICHARG>10` as `LSKIP_POSCORR`, so fixed-density postprocessing preserves the CHGCAR grid exactly while ordinary non-fixed reads keep the original correction behavior.
- Rebuilt the local diagnostic binary after the `READCH` skip-position-correction patch.
- Submitted `work/cal18_skipposcorr_contcar_nsw10`, using high-precision `CONTCAR` positions, fixed-density `ICHARG=11`, and `NSW=10`, to test whether this simultaneously matches CHGCAR density, `DENCOR`, cavity, and the converged solute-potential input.
- Because `work/cal18_skipposcorr_contcar_nsw10` was pending on `gpu-a100-dev`, also submitted the same test as `work/cal18_skipposcorr_contcar_nsw10_dev` on the `development` partition.

## 2026-06-29 skip-poscorr + CONTCAR fixed-density reproduction test
- Run: `work/cal18_skipposcorr_contcar_nsw10_dev` using patched local CEP-DIP binary.
- Inputs: high-precision `CONTCAR` positions for ionic/DENCOR terms; fixed-density `CHGCAR`; `READCH(..., LSKIP_POSCORR=.TRUE.)` to avoid applying old-position/new-position correction to fixed `CHTOT`; `CHTOT_READ` restored before final potential and dipole postprocessing.
- Compared against local full-SCF reference `work/cal18_full_scf_dbg`.
- Debug input fields:
  - `DBG_NVAL`: integral exactly equal; raw RMSE `1.15848655658e-10`.
  - `DBG_DENCOR`: exactly equal within printed volumetric file precision; raw RMSE `0`.
  - `DBG_NE`: raw RMSE `6.62614505049e-08`, max abs `1.00000761449e-06`, consistent with volumetric output precision.
  - `DBG_PHISOL_IN`: raw RMSE `1.2903942149e-06 eV`, max abs `2.22303000008e-06 eV`.
- Final fields:
  - `PHI`: raw RMSE `1.24345572658e-06 eV`.
  - `PHISOLV`: raw RMSE `8.03962137013e-07 eV`.
  - `RHOB`: raw RMSE `9.69560156223e-05`, z-mean RMSE `2.34025933426e-05`.
  - `RHOION`: raw RMSE `1.70596324113e-06`, z-mean RMSE `1.23950040795e-06`.
  - `SION/SDIEL/SSOLV/SCAV`: raw RMSE about `1e-13`, i.e. identical at file precision.
- Interpretation: fixed density, DENCOR, total PB input density, and cavity fields are now reproduced. Remaining solvent-charge differences track a residual `~1e-6 eV` difference in the potential passed into the PB solve, so next debugging targets PB/dipole iterative convergence/state rather than density construction.

## 2026-06-29 original cal_18 CHGCAR fixed-density reproduction, NSW=10
- Run: `work/cal18_original_chgcar_skipposcorr_nsw10`.
- Inputs are original `cal_18/CHGCAR`, original `cal_18/CONTCAR` copied as `POSCAR`, original `POTCAR/KPOINTS`, and fixed-density `INCAR` (`ICHARG=11`, `NELM=0`, `NSW=10`). No original solvent output is used as input.
- `DBG_NVAL` vs original `CHGCAR`: integral exactly equal; raw RMSE `1.37513263653e-12`.
- Final output vs original `cal_18`:
  - `PHI`: raw RMSE `1.41556027649e-07 eV`, z-mean RMSE `1.40934234264e-07 eV`.
  - `RHOB`: raw RMSE `2.07512196558e-05`, z-mean RMSE `1.10761376089e-05`.
  - `RHOION`: raw RMSE `2.80804779934e-07`, z-mean RMSE `1.27296091447e-07`.
- Interpretation: direct original-output reproduction is close but `RHOB` remains above desired numerical noise. Submitted `NSW=20` repeat (`work/cal18_original_chgcar_skipposcorr_nsw20`, job 3264913) to test whether remaining difference is PB outer-state convergence.

## 2026-06-29 original cal_18 CHGCAR fixed-density reproduction, NSW=20
- Run: `work/cal18_original_chgcar_skipposcorr_nsw20` (job 3264913).
- Same original `CHGCAR/CONTCAR/POTCAR/KPOINTS`; only fixed-density outer steps changed from `NSW=10` to `NSW=20`.
- `DBG_NVAL` vs original `CHGCAR`: integral exactly equal; raw RMSE `1.37513263653e-12`.
- Final output vs original `cal_18`:
  - `PHI`: raw RMSE `1.69680267419e-07 eV`, z-mean RMSE `1.69120428849e-07 eV`.
  - `RHOB`: raw RMSE `2.06883879454e-05`, z-mean RMSE `1.09030521245e-05`.
  - `RHOION`: raw RMSE `2.74730062985e-07`, z-mean RMSE `1.22649241424e-07`.
- Interpretation: increasing outer steps from 10 to 20 does not reduce the remaining `RHOB` mismatch. Remaining difference is not simple outer-iteration underconvergence; next checks target code-version parity and `RHOB` write/state path.

## 2026-06-29 local LS6 build of Oct-2025-like `CEP-implicit-ion` source
- Copied `/work/08384/tg876840/stampede3/CEP-implicit-ion` to `code/CEP-implicit-ion-ls6` and replaced `makefile.include` with the LS6 `CEP-DIP` build configuration so it can compile/run on the current machine.
- Reason: original NiN `cal_18` was run with `/anvil/projects/x-che190065/rywang/CEP-HALF/bin/vasp_std` (build Oct 02 2025). That binary is unavailable on LS6. The closest local source/binary found is `CEP-implicit-ion` (build Oct 01 2025), but its prebuilt binary cannot run on LS6 due missing/incompatible runtime libraries.
- Patched `code/CEP-implicit-ion-ls6/src/fileio.F`:
  - Added optional `LSKIP_POSCORR` argument to `READCH`.
  - Added `LDO_POSCORR` local flag; default behavior unchanged.
  - When `LSKIP_POSCORR=.TRUE.`, skip the old-position/new-position atomic density subtraction/addition, and only deallocate `T_INFO_OLD%POSION`.
- Patched `code/CEP-implicit-ion-ls6/src/main.F`:
  - Added `CHTOT_READ(:,:)` and `LHAVE_CHTOT_READ`.
  - Pass `INFO%ICHARG>10` to `READCH` so fixed-density runs preserve the CHGCAR grid exactly.
  - Save `CHTOT_READ=CHTOT` after successful fixed-density CHGCAR read.
  - Restore `CHTOT=CHTOT_READ` before final `POTLOK` and before `CDIPOL_CHTOT_REC`.
- Patched `code/CEP-implicit-ion-ls6/src/solvation.F`:
  - When `LWRITE`, write `DBG_NVAL`, `DBG_DENCOR`, `DBG_NE`, `DBG_PHISOL_IN` immediately before the PB solver tolerance setup.
- These patches are diagnostic/fixed-density-read protections only; the old `CEP-implicit-ion` solvent model formulas are otherwise left intact for comparison.
2026-06-29:
- Patched `code/CEP-implicit-ion-ls6/src/force.F` in `FORCE_AND_STRESS`:
  changed `IF ( DIP%IDIPCO >0 ) THEN EIFOR=EIFOR+DIP%FORCE` to
  `IF ( DIP%IDIPCO >0 .AND. ASSOCIATED(DIP%FORCE) ) THEN ...`.
  Reason: fixed-density runs with `LDIPOL=.TRUE.` reached `FORCE_AND_STRESS`
  before `DIP%FORCE` was allocated, causing SIGSEGV. This matches the guarded
  logic in the current LS6 `CEP-DIP` source and only skips an invalid access
  when no dipole-force array exists.
- Temporarily changed `code/CEP-DIP/src/solvation.F` `tol_init` in `SOL_VCORRECTION` from `1.e-3_q` to `4.048906203422019e-6_q`. Reason: diagnostic test to see whether the remaining original-CHGCAR fixed-density `RHOB` mismatch is caused by using a much tighter PB state than the original final SCF call (`rms_SCF=4.048906203422019e-5`, `tol=rms_SCF/10`). This change is not final and must be reverted if it does not improve reproduction.
- Rebuilt local `code/CEP-DIP/bin/vasp_std` after the temporary forced-tolerance diagnostic change.

- Reverted `code/CEP-DIP/src/solvation.F` `tol_init` from `4.048906203422019e-6_q` back to `1.e-3_q` after `work/cal18_original_chgcar_forcetol_nsw20` completed. Result: the forced-tolerance run did not reproduce original `RHOB`; versus original `cal_18`, `PHI` raw RMSE was `1.7069e-7`, `RHOB` raw RMSE was `2.0653e-5`, and `RHOION` raw RMSE was `2.7584e-7`. It matches the normal fixed-density run closely (`RHOB` raw RMSE `1.5942e-7` versus `work/cal18_original_chgcar_skipposcorr_nsw20`), so tolerance/history after convergence is not the remaining fix.

- Build note: attempted top-level `make -C code/CEP-DIP -j 8` after reverting `tol_init`. The `std` binary was regenerated (`code/CEP-DIP/bin/vasp_std`, timestamp Jun 29 12:29), but the top-level command returned nonzero because it also tried `gam` and `ncl` targets and failed in unrelated module compilation. Future rebuilds should use the `std` target only for this workflow.

- Temporary diagnostic change: added numbered `RHOB_NLPB_####` writes inside `NLPCM_solver` LWRITE block in `code/CEP-DIP/src/solvation.F`. Purpose: determine whether fixed-density postprocessing passes through an `RHOB` state closer to original `cal_18/RHOB`. This is not final and must be reverted after the snapshot comparison.

- Removed the temporary `RHOB_NLPB_####` snapshot writes from `code/CEP-DIP/src/solvation.F`. Diagnostic run `work/cal18_rhob_snap_nsw12` produced exactly one snapshot during final field write; `cmp` showed `RHOB_NLPB_0001` and final `RHOB` were byte-identical, so this did not expose intermediate PB states. Deleted the 189 MB snapshot file.

- Temporary diagnostic change: added independent per-call `RHOB_CALL_####` writes at the end of `NLPCM_solver`, limited to the first 16 calls and not gated by `LWRITE`. Purpose: test whether intermediate fixed-density PB states pass closer to original `cal_18/RHOB` than the final converged fixed-density state. This is not final and must be removed after comparison.

- Diagnostic result for per-call `RHOB_CALL_####` snapshots in `work/cal18_rhob_call_dev30`: 13 PB calls were captured. The best match to original `cal_18/RHOB` was the final call (`RHOB_CALL_0013`) with raw RMSE `2.068873076e-5`, z-mean RMSE `1.099198616e-5`, and max abs `0.0018570198`. Earlier calls were worse (`1.281`, `0.414`, `0.231`, `0.0817`, `0.00666`, `0.00122`, `0.00130`, `1.30e-4`, then ~`2.1e-5`). Therefore the original `RHOB` mismatch is not explained by passing through a better intermediate fixed-density PB state. Final `PHI` raw RMSE was `1.6007e-7` and final `RHOION` raw RMSE was `2.7814e-7`.

- Removed the temporary independent per-call `RHOB_CALL_####` writes from `code/CEP-DIP/src/solvation.F` after the snapshot comparison; rebuilt `std` afterward.

- Copied `/work/08384/tg876840/stampede3/CEP-ion` to `code/CEP-ion` as another Oct-2025-adjacent source comparison. Replaced only `makefile.include` with the known-working LS6 build configuration from local `code/CEP-DIP`.
- Initial `code/CEP-ion` build failed because the copied `build/std` directory contained object files from a different runtime/compiler environment. Removed `code/CEP-ion/build` and `code/CEP-ion/bin`, then rebuilt `std` from source successfully. This is a build-environment cleanup only; no physical source logic was changed.
- Prebuilt `/work/08384/tg876840/stampede3/CEP-ion/bin/vasp_std` could not run on LS6 because it required incompatible/missing dynamic libraries (`libmkl_*.so.2`, newer `GLIBC/GLIBCXX`). It is not usable for numerical comparison on this machine.
- Submitted `work/cal18_cepion_lsbld_nsw20` using the local LS6-built CEP-ion binary on `gpu-a100-small`; it started but crashed in MPI all-to-all/SIGSEGV before producing solvent fields. This is treated as a node/runtime failure, not a physical result.
- Submitted `work/cal18_cepion_lsbld_dev_nsw20` on the `development` partition with the same inputs and LS6-built CEP-ion binary for the actual comparison.
- Prepared `work/cal18_cepsolvpp_bin_dev_nsw20` using the same inputs and the LS6-native `/work/08384/tg876840/ls6/CEP-vaspsol++/bin/vasp_std` binary. `ldd` resolved its dependencies on LS6.
- Tried to submit `work/cal18_cepsolvpp_bin_dev_nsw20` to `vm-small` with 20 MPI ranks, but Slurm rejected the node configuration before job creation; removed the invalid `run_vm.slurm`.
- Submitted `work/cal18_cepsolvpp_bin_dev_nsw20` on the `development` partition as job 3265600.
- Submitted an additional independent CEP-ion comparison directory `work/cal18_cepion_lsbld_a100_nsw20` on the `gpu-a100` partition as job 3265610 to increase scheduling chances. It uses the same inputs and local LS6-built CEP-ion binary; duplicate queued/running jobs should be canceled after the first useful result is obtained.
- Canceled unpatched queued comparisons 3265592, 3265600, and 3265610 before they started. Reason: the CEP-ion binary had not yet received the fixed-density `READCH` skip-position-correction and `CHTOT_READ` restoration patches, so any result would confound code-version differences with fixed-density read-path differences. `CEP-vaspsol++` also lacks the `LVAC/SOL_Z0/SOL_Z1` logic used by the original input, so it is at best a negative control.
- Patched `code/CEP-ion/src/fileio.F`: added optional `LSKIP_POSCORR` to `READCH`, with default behavior unchanged; fixed-density callers can skip the old-position/new-position atomic density correction while still deallocating `T_INFO_OLD%POSION`.
- Patched `code/CEP-ion/src/main.F`: added `CHTOT_READ`/`LHAVE_CHTOT_READ`, passed `INFO%ICHARG>10` to `READCH`, saved the fixed-density CHGCAR charge after successful read, and restored it before the final solvent `POTLOK` and `CDIPOL_CHTOT_REC` calls.
- Patched `code/CEP-ion/src/solvation.F`: when `LWRITE`, write diagnostic PB inputs `DBG_NVAL`, `DBG_DENCOR`, `DBG_NE`, and `DBG_PHISOL_IN` immediately before solver dispatch. This is diagnostic output only and not used as input.
- Rebuilt `code/CEP-ion/bin/vasp_std` successfully after these patches.
- Submitted patched CEP-ion fixed-density comparisons `work/cal18_cepion_patch_dev_nsw20` (job 3265625, development) and `work/cal18_cepion_patch_a100_nsw20` (job 3265626, gpu-a100).
- Removed obsolete/no-result run directories after recording their purpose: `work/cal18_cepion_nsw12` (prebuilt binary missing/incompatible libraries), `work/cal18_cepion_lsbld_nsw20` (gpu-a100-small runtime crash), `work/cal18_cepion_lsbld_dev_nsw20` and `work/cal18_cepion_lsbld_a100_nsw20` (unpatched queued comparisons canceled before use), and `work/cal18_cepsolvpp_bin_dev_nsw20` (negative-control candidate canceled before use because it lacks the needed LVAC input logic).
- Tried an additional patched CEP-ion submission to `NuclearEnergy-dev`, but Slurm rejected it with an invalid account/partition combination before job creation. Removed `work/cal18_cepion_patch_nucdev_nsw20`.
- Patched `code/CEP-ion/src/force.F`: changed the dipole-force addition from `IF (DIP%IDIPCO >0) EIFOR=EIFOR+DIP%FORCE` to `IF (DIP%IDIPCO >0 .AND. ASSOCIATED(DIP%FORCE)) ...`. Reason: a small-node `NSW=2` smoke test crashed in `force_and_stress_` because `DIP%FORCE` was not allocated in this fixed-density path. This guard matches the current CEP-DIP source and prevents an invalid pointer access without changing cases where `DIP%FORCE` exists.
- Rebuilt `code/CEP-ion/bin/vasp_std` successfully after the force guard.
- Replaced the failed `work/cal18_cepion_patch_small_smoke` directory and submitted a new `NSW=2` smoke test with the rebuilt binary as job 3265651 on `gpu-a100-small`.
- Smoke test 3265651 entered `NLPCM_solver` but was killed on `gpu-a100-small` (signal 9/11) before any solvent fields were written; the output showed only 4 processes per KPAR group and no useful comparison fields. Treated as small-node/runtime limitation, not a numerical result. Removed the smoke directory.
- Recreated patched CEP-ion `NSW=20` comparison directories with the force-guarded binary and submitted jobs 3265669 (`development`) and 3265670 (`gpu-a100`).
- Added a third independent patched CEP-ion `NSW=20` comparison directory `work/cal18_cepion_patch_normal_nsw20` and submitted it to `normal` as job 3265674 to increase scheduling chances. It uses the same input and binary as the development/gpu-a100 submissions.
- Tested shorter 1-hour development/gpu-a100 submissions to improve backfill, but Slurm reported `Resources` and no earlier start estimate. Canceled jobs 3265692 and 3265693 and removed `work/cal18_cepion_patch_dev1h_nsw20` and `work/cal18_cepion_patch_a1001h_nsw20`.
- Patched CEP-ion `NSW=20` comparison job 3265670 on `gpu-a100` reached final field output. Canceled duplicate pending jobs 3265669 and 3265674 after the first useful result was available.
- Result: CEP-ion is not the original-source match. Versus original `cal_18`, `CHGCAR` is byte/numerically identical, but `PHI` raw RMSE is `2.71402468716e-4 eV`, `RHOB` raw RMSE is `4.2299815109e-2`, and `RHOION` raw RMSE is `3.38754973163e-4`.
- Diagnostic entry fields versus the CEP-DIP fixed-density run `work/cal18_original_chgcar_skipposcorr_nsw20`: `DBG_NVAL`, `DBG_DENCOR`, and `DBG_NE` are exactly identical, while `DBG_PHISOL_IN` already differs with raw RMSE `1.7213112991e-5`. Therefore the CEP-ion mismatch is not a CHGCAR/DENCOR read issue; it is a different solvent-potential/source path before or inside the PB solve. This branch is a negative result and should not be used for final reproduction.
- Compared visible CEP source trees. `CEP-planer/CEP-planar` do not contain the `LVAC/SOL_Z0/SOL_Z1` solvent mask logic needed by the original input. `stampede3/CEP-test` has matching `LVAC` solvent logic, but its `solvation.F` differs from current CEP-DIP only by debug/extra-output lines in the relevant `NLPCM_solver/SOL_VCORRECTION` path, so it cannot explain the remaining `RHOB` mismatch. `stampede3/CEP-DIP` and `ls6/CEP-DIP` have identical `solvation.F` and `main.F`; their only relevant difference found is in `dipol.F`.
- Created diagnostic source tree `code/CEP-DIP-old_dipol`: copied only `src`, `arch`, `makefile`, `makefile.include`, and `README.md` from current patched `code/CEP-DIP`, then replaced only `src/dipol.F` with `/work/08384/tg876840/stampede3/CEP-DIP/src/dipol.F`. This isolates the old z-only dipole correction while keeping fixed-density CHGCAR read protection and solvent code unchanged.
- Build note for `code/CEP-DIP-old_dipol`: an initial full `cp -a code/CEP-DIP` was interrupted because it was copying the large `build` tree unnecessarily. Removed the partial copy and recreated the tree with source/build inputs only. A clean `make -j8` failed from Fortran module ordering; `make -j1` compiled and linked successfully, with only the final copy failing because `bin/` did not exist. Created `bin/` and copied `build/std/vasp` to `bin/vasp_std`.
- Submitted old-dipole fixed-density comparison `work/cal18_old_dipol_nsw20` as job 3265961 on `development`. Inputs match `work/cal18_original_chgcar_skipposcorr_nsw20`; only the binary differs.
- Because job 3265961 had no estimated start time, submitted an isolated `normal` backup with identical inputs and binary in `work/cal18_old_dipol_nsw20_normal` as job 3265977. Keep whichever finishes first and cancel/delete the unused duplicate.
- Additional source search found `/work/08384/tg876840/stampede3/software/duy_le/solvation.F` dated 2025-08-24, but it does not contain the current `LVAC/SOL_Z0/SOL_Z1` solvent-mask implementation used by the original `cal_18` input. Excluded it from testing.
- Submitted an additional short `gpu-a100-small` opportunity test with identical inputs and binary in `work/cal18_old_dipol_nsw20_small` as job 3265999 using 4 ranks and 1 hour. This partition previously killed a CEP-ion PB test, so this is only an opportunistic run; delete it if it fails or if another duplicate finishes first.
2026-06-29 17:45
- Added `work/cal18_old_dipol_nsw20_a100dev/run.slurm` and copied the same fixed-density inputs plus `code/CEP-DIP-old_dipol/bin/vasp_std` into that run directory.
- Purpose: run the old `dipol.F` diagnostic on a faster available GPU dev node. This is only a numerical-source isolation test for the remaining `RHOB` mismatch; it does not change the production reproduction workflow.
- Canceled and removed `work/cal18_old_dipol_nsw20_small` after it reached only step 2 in ~17 minutes; the `gpu-a100-dev` run with identical inputs was already much faster, so the small run was only clutter.
- `work/cal18_old_dipol_nsw20_a100dev` completed. Compared against the original `cal_18`: `PHI` raw RMSE `1.69680267419e-07`, `RHOB` raw RMSE `2.06883879454e-05`, `RHOION` raw RMSE `2.74730062985e-07`, identical to `work/cal18_original_chgcar_skipposcorr_nsw20`.
- Compared old-dipole output against current best: `PHI`, `RHOB`, `RHOION`, `DBG_NVAL`, `DBG_DENCOR`, `DBG_NE`, and `DBG_PHISOL_IN` are all bitwise identical by the grid comparison script. Conclusion: the old `dipol.F` variant is not the source of the remaining `RHOB` mismatch.
- Canceled duplicate pending/running fallback jobs `3265961` and `3265977`; removed the old-dipole diagnostic code/run directories after recording the negative result.

2026-06-29 18:10
- Checked the remaining `RHOB` mismatch spatially: bottom/vacuum regions are essentially zero, while the z-mean error is concentrated at the solvent/interface region around z = 24.5-28.8 A and local raw error also appears around z = 12.2-12.6 A.
- Verified the fixed-density run writes a `CHGCAR` that is byte-for-byte identical to the original `cal_18/CHGCAR`, including the augmentation occupancy section (`cmp_exit=0`, both files 197346528 bytes). The CHGCAR grid is `(168,168,384)` and there are 1321 non-grid lines after the first charge-density block.
- Compared the magnitude of the residual potential curvature with the `RHOB` residual: `dphi` raw RMSE is `1.69680267419e-07 eV`, z-mean second-derivative RMSE is `5.94622456e-07 eV/A^2`, and `RHOB` z-mean RMSE is `1.09030521246e-05`. This supports the interpretation that tiny potential differences at cavity gradients can produce the remaining bound-charge mismatch.

2026-06-29 18:18
- Added `work/cal18_original_chgcar_nsw1` as a minimal fixed-density path test using the same byte-identical original `CHGCAR`, current patched `code/CEP-DIP/bin/vasp_std`, and `NSW=1` instead of `NSW=20`.
- Purpose: check whether the remaining `PHI/RHOB` mismatch comes from repeated static ionic steps / solver path rather than from the fixed CHGCAR itself.
- Result: `NSW=1` is not a valid reproduction path. Against original `cal_18`, `PHI` raw RMSE was `0.161054355474 eV`, `RHOB` raw RMSE was `0.413521750136`, and `RHOION` raw RMSE was `0.0626579965783`. The solvent dipole was also far from the final value (`Dsol_z ~0.278` instead of ~0.683). Removed the run directory after recording the negative result.

2026-06-29 18:27
- Cleaned large failed/diagnostic artifacts. `work/` now keeps only `cal18_original_chgcar_skipposcorr_nsw20`, the best fixed-CHGCAR reproduction. `code/` now keeps only `CEP-DIP`, the patched source tree used for that reproduction.
- Kept small text diagnostics in `results/` and this change log so the discarded tests remain traceable without keeping tens of GB of volumetric outputs.

2026-06-29 23:40
- Added `FIXSOL_STEPS` support in `code/CEP-DIP/src/main.F` for fixed-density CHGCAR solvent-output runs. When `ICHARG>10`, a saved `CHTOT_READ`, and `LSOL` are present, the code repeats `POTLOK` with `SET_SOL_NOWRITE` for `FIXSOL_STEPS-1` iterations and then runs one final `SET_SOL_WRITE` iteration. This avoids using ionic `NSW` steps solely to relax the solvent/dipole state.
- Submitted `step_scan/step01` through `step_scan/step05` on `gpu-a100-dev` with `FIXSOL_STEPS=1..5`. All five completed. Summary files:
  - `results/checks/cal18_step_scan_summary.tsv`
  - `results/profiles/cal18_step_scan_profiles.tsv`
- Result versus original DFT `cal_18`: `PHI` raw RMSE falls from `1.6105e-1` at 1 step to `1.0718e-4` at 5 steps; `RHOB` raw RMSE falls from `4.1352e-1` to `1.2171e-3`; `RHOION` raw RMSE falls from `6.2658e-2` to `4.8635e-5`. Five steps are substantially converged but still less accurate than the existing 15-step fixed-density reproduction.
- Fixed a diagnostic-output bug in `code/CEP-DIP/src/dipol.F`: the temporary `FIXSOL_CDIPOL_*` writes used `IU0`, but `CDIPOL` does not receive `IU0`; that was an uninitialized implicit integer. Replaced these conditions with `GRIDC%COMM%NODE_ME==GRIDC%COMM%IONODE` and stdout writes. This changes diagnostic printing only, not the dipole/PB calculation.
- Rebuilt `code/CEP-DIP/bin/vasp_std` after the diagnostic-output fix.

2026-06-30 cleanup
- Removed old full VASP run directories from the top level: `clean_minimal_chgcar_pb`, `fast_fixed_chgcar_pb`, `fast_minout_chgcar_pb`, `fast_solonly_chgcar_pb`, `fast_solonly10_chgcar_pb`, `fast_solonly15_chgcar_pb`, `old_cepdip_scf_125`, and `step_scan`. Their useful numerical results are preserved in compact text/TSV summaries under `results/` and `reproduce3d/results/`.
- Removed `reproduce3d/work` because it only contained large raw run outputs; the source, input case, summaries, and change history are kept.
- Removed obsolete standalone 1D PB approximation code/output: `src/pb1d.py`, `tests/check_pb_components.py`, `tests/check_pb_scf.py`, `tests/check_vasp_cavity_projection.py`, and the corresponding `results/checks` and `results/profiles` files. These were negative tests, not a validated reproduction route.
- Removed Au-only cavity projection data `data/case_au_cal18`; it was only used by the deleted negative 1D approximation test.
- Removed `data/case_cal18/AECCAR0` and the old `tests/build_reference_profiles.py`/reference summary files because the current validated VASP fixed-CHGCAR reproduction uses `POTCAR`/VASP source logic for DENCOR, not AECCAR0 as an input proxy.
- Added missing `data/case_cal18/KPOINTS` from the original `cal_18` input so the retained NiN case is a complete minimal VASP input/reference set.
- Removed `reproduce3d/code/CEP-DIP/build` and `reproduce3d/code/CEP-DIP/testsuite` to keep only source, make files, patches, and `bin/vasp_std`.
- Added root `README.md` and rewrote `notes/cep_dip_1d_pb_findings.md` so retained documentation describes the current validated workflow and does not point to removed failed scripts.

2026-06-30 detailed VASP NLPB timing instrumentation
- Edited `code/CEP-DIP/src/solvation.F` for timing only; no physical expressions, convergence criteria, solver order, or field outputs were changed.
- Added fine-grained `STOP_TIMING("SOL", IU6, ...)` labels in `SOL_VCORRECTION`:
  `ALLOC`, `NVAL_COPY`, `DENCOR_NE`, `QSOL`, `PHISOL_IN`, `DBG_WRITE`, and `TOL`.
  The original aggregate `INIT` and `FINALIZE` labels are retained.
- Added fine-grained NLPB solver labels in `NLPCM_solver`:
  `NLPB_CAVITY`, `NLPB_UPDATE0`, `NLPB_RESID`, `NLPB_SETLPB`, `NLPB_MINL`,
  `NLPB_UPDPHI`, `NLPB_SOLPOT`, and `NLPB_WRITE`.
- Added `Update_NLPB` internal timing labels:
  `UPD_ELOC`, `UPD_ION`, `UPD_DIEL`, `UPD_BOUND`, and `UPD_ENERGY`.
- Added `SOLVE_E_LOC` internal timing labels:
  `ELOC_GRAD`, `ELOC_FZERO`, and `ELOC_SCALE`.
- Added `IU6` to the `solvation_nlpcm` module state and set `IU6 = IO%IU6` in
  `NLPCM_READER`, matching the existing LPB module pattern so NLPB timing writes to OUTCAR.
- Added `USE ini, ONLY: STOP_TIMING` to `solvation_nlpcm` so the timing calls bind to
  VASP's profiling interface instead of an unresolved external symbol.
- Compilation diagnostics:
  first compile failed because `IU6` was not in scope inside `solvation_nlpcm`;
  second compile failed because `STOP_TIMING` was not imported into `solvation_nlpcm`.
  Both failures were timing-interface issues only and were fixed before testing.
- Rebuilt `code/CEP-DIP/bin/vasp_std` successfully after these timing-only changes.
- Added `tests/parse_sol_timing.py` to parse OUTCAR timing lines into
  `sol_timing_events.tsv` and `sol_timing_summary.tsv`.
- The first completed timing run showed VASP truncates timing labels to eight
  characters, which made long labels such as `NLPB_UPDATE0` and `NLPB_UPDPHI`
  ambiguous. Replaced all newly added labels with unique labels of at most eight
  characters: `ALLOC`, `NVALCPY`, `DENCOR`, `QSOL`, `PHISOL`, `DBGWRIT`, `TOL`,
  `NCAVITY`, `NUPD0`, `NRESID`, `NSETLPB`, `NMINL`, `NUPDPHI`, `NSOLPOT`,
  `NWRITE`, `UELOC`, `UION`, `UDIEL`, `UBOUND`, `UENERGY`, `EGRAD`, `EFZERO`,
  and `ESCALE`.
- Rebuilt `code/CEP-DIP/bin/vasp_std` after the label correction.
- Added detailed Python-stage timing without changing numerical logic:
  `pure_python/solve_from_chgcar_newton.py` now writes `stage_detail_times.tsv`
  in addition to `stage_times.tsv`; `pure_python/solute_potential.py` accepts an
  optional timing list and records `solute_fft_valence`,
  `solute_structure_factors`, `solute_dencor`, `solute_hartree`, and
  `solute_local_potential`; `pure_python/pb.py` accepts optional cavity timing
  and records `cavity_vdw_shape`, `cavity_kernels`, `cavity_ion`,
  `cavity_solv`, `cavity_cav`, `cavity_diel`, and `cavity_vacuum_mask`.
  These edits only record timing and do not change formulas or solver order.
- The first Python detailed-timing run was canceled because
  `stage_detail_times.tsv` double-counted the full solute block in
  `solute_ifft_cvhar`; this was a timing-cursor bug only. Fixed
  `extend_detail()` to reset the detail timer after appending nested timings,
  deleted the invalid output directory, and reran the test.
- Completed Python detailed-timing job `3268900` in
  `pure_python/results/from_chgcar_newton_detail5`. Wall time was `00:17:40`.
  Coarse stage times were `read_inputs 13.714 s`,
  `solute_potential_and_dencor 5.471 s`, `create_cavity 4.325 s`,
  then fixsteps `607.512 s`, `150.876 s`, `84.549 s`, `84.595 s`,
  and `102.860 s`.
- The corrected Python detail timing showed read split:
  `read_chgcar 3.232 s`, `read_phi_ref 3.170 s`, `read_rhob_ref 3.198 s`,
  `read_rhoion_ref 4.111 s`, `read_potcar 0.031 s`;
  solute split: `solute_dencor 2.296 s`,
  `solute_local_potential 1.855 s`, `solute_structure_factors 0.648 s`,
  `solute_fft_valence 0.183 s`, `solute_hartree 0.169 s`,
  `solute_ifft_cvhar 0.195 s`; cavity split:
  `cavity_kernels 1.037 s`, `cavity_ion 0.716 s`,
  `cavity_solv 0.726 s`, `cavity_diel 0.730 s`,
  `cavity_vacuum_mask 0.729 s`, `cavity_vdw_shape 0.367 s`,
  and `cavity_cav 0.011 s`.
- Added one VASP outer read/setup timing in `code/CEP-DIP/src/main.F`:
  `START_TIMING("RDINP")` immediately after opening `CHGCAR`, and
  `STOP_TIMING("RDINP", IO%IU6, "RDINPUT")` at the existing code boundary
  labeled "At this very point everything has been read in". This is a
  timing-only change.
- Rebuilt `code/CEP-DIP/bin/vasp_std` after adding `RDINPUT`.
- Completed `timing_cal18_fix5_rdinp` as job `3268950` on `development`.
  Wall time was `00:06:03`; `job.e` is empty and `PHI`, `RHOB`, and `RHOION`
  were written. Parsed files are `timing_cal18_fix5_rdinp/sol_timing_events.tsv`
  and `timing_cal18_fix5_rdinp/sol_timing_summary.tsv`.
- Key VASP timing labels from `3268950`: `RDINPUT 4.380 s`, `NCAVITY 4.912 s`,
  `DENCOR 0.303 s`, `PHISOL 0.059 s`, `QSOL 0.002 s`,
  `NMINL 92.362 s`, `NSOLPOT 3.358 s`, and `NWRITE 22.567 s`.
